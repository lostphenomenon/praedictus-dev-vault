console.log({ a:1, b:2});

// Prints the object out as a fancy table
console.table({ a:1, b:2});

// Prints out messages
console.error('this is some error!');
console.warn('this is some warning!');

// Clear console
console.clear();

// Measure time through console between two
// points in time. You have to provide the same 
// identifier
console.time('identifier');
console.timeEnd('identifier')